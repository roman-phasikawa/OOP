String is a class in java which is used to store a sequence of characters in the form of a character which may contain alpha numeric value enclosed in double quote.

Methods of creating Strings:-1.String literal 2.new Keyword

String methods:
length(), replace(char oldLetter, char newLetter), toLowerCase(), toUpperCase(), trim(), concat(String newString), split()
, comnpareTo(),IsEmpty().

